# BatchLens — differentiation and scientific validation

> 2026-09-12 状态更新：用户已授权完成首个项目并创建/上传仓库；受限 MVP 已实现，GitHub/PyPI v0.1.0 已发布，CI 与正式索引干净安装回验通过。以下保留规划背景；当前行为以 README、docs/input-schema.md、docs/methods.md 和 docs/decisions.md 为准。外部 G0/G1 验证并未完成。

2026-09-11。目的：决定它值得成为独立工具，还是更适合一篇 recipe / 上游扩展。当前状态：桌面竞品研究完成；真实任务比较、用户试用和独立统计复核未完成。这里是事先写下的验证门，不是已经取得的结果。

## 1. 可证伪的产品假设

H1：单细胞/空间分析者反复需要检查实验单位与技术观测关系，通用矩阵工具需要额外准备和人工说明。

H2：将 contrast 可估计性、重复层级、数据冲突和稳定 JSON findings 放进一次本地体检，比一个普通 notebook 有明显交付与复跑价值。

H3：用户正确理解 NON_ESTIMABLE、ESTIMABLE、NOT_ASSESSED 的差别，不会把报告当作数据或因果结论的合格证。

最强反对意见：这些检查本身不复杂，已有 R 工具和几行线性代数就能覆盖；领域包装未必值得另一套长期依赖与维护。必须正面检验。

## 2. 不编码也能审核的报告样例

**案例 A：time 与 run 完全混杂。** 四个独立 units；两条 D0/A、两条 D7/B。按 `[intercept,time_D7,run_B]` 编码，X 的两类行为 `[1,0,0]` 和 `[1,1,1]`，rank=2，p=3。存在零空间向量 `[0,1,-1]`；D7−D0 的 c=`[0,1,0]` 与它内积非零。因此目标不可估计。增加相同行的细胞/spot 数不改变这个结论。

期望报告：`BL-CONTRAST-001 / critical / NON_ESTIMABLE`；附 2×2 样本/units 表；说明现有设计无法在此模型中分离 time/run，讨论交叉批次采样或修改科学问题，而非“先 Harmony 再分析”。

**案例 B：不均衡不等于完全混杂。** D0/A 两个 units、D7/A 一个、D7/B 两个。行有 `[1,0,0]、[1,1,0]、[1,1,1]`，rank=3；D7−D0 在加性模型中可估计。缺 D0/B 仍须报告 coverage warning，并解释估计借助无交互等模型假设。不能因一个空格就宣布所有比较无效。

**案例 C：矩阵秩亏但目标仍可估计。** target 与 batch 完全交叉，技术字段 run 与 machine 重复编码。X=`[1,target,run,machine]`，run=machine，rank=3<4；target 的 contrast 不涉及这条依赖，仍可估计。报告同时有冗余 nuisance finding 和可估计目标，不能只亮红灯。

**案例 D：空间重复。** 3 subjects、每人 4 sections，共 12 samples；再增加 spots。显示 subjects=3、samples=12，不声称 n=12 个独立人。若样本级模型超出首版 independent/paired 契约，输出 NOT_ASSESSED 和完整层级证据；不能为了给出结论自动丢弃 9 张切片。

这些是人为设定的数学/契约样例，不是公开数据实测或用户价值证明。

## 3. 与最近替代品的公平任务比较

准备同一组去标识 metadata、相同 target/contrast、预先定义的正确答案；给每个工具合理教程时间。对照：BatchQC、ExploreModelMatrix、一份包含交叉表和矩阵检查的普通 notebook。先比较纸面输入/报告契约，进入受控实现后再比较实际运行；不故意禁用竞品已有静态导出/脚本接口。

记录任务完成时间、需要手动整理的步骤、正确识别目标受限的比例、能否复跑和交付；不得把没有运行的对照填成“失败”。若无可运行 BatchLens，只能验证报告理解与需求，不能宣称速度胜出。

## 4. 两道门，避免先造产品再找理由

### G0：允许受控实现的前置门

- [x] 用户定位和首版任务明确；scIB、BatchQC、ExploreModelMatrix 等已研究。
- [x] 书面 MVP、CLI、数据契约、科学反例、架构、Release DoD 已定义。
- [ ] 至少 2 个真实场景被逐项记录，其中一个来自用户的 sample/time/batch 问题；不能用 synthetic 替代真实需求。
- [ ] 至少 2 位目标使用者（可包含用户）阅读报告样例；都能正确解释不可估计与功效/因果效应的区别；至少一位明确有重复使用需求。
- [ ] 对现有工具能否用轻量模板完成同一工作做实操评估，记录具体缺口。
- [ ] 具备线性模型经验的独立 reviewer 审阅上述四类判断与措辞，无未解决的科学阻断问题。

仅当这些证据支持 H1/H2/H3 才开始受控实现。此门来自用户“没有很强答案不要急着编码”的要求；本次不把缺少实证写成已通过。

### G1：MVP 后独立发行的价值门

至少 3 位目标用户（含至少 1 位空间组学用户）在相同输入/任务下试用；相对最合适基线，至少 2 位有明确的复用意愿，并记录一个可复现的效率或错误预防收益；所有人都不能因报告误判完全混杂为“可校正”。这是早期筛选阈值，不是统计推断的市场验证。

达不到 G1：优先收窄、修正文案或转 recipe；不要靠加 LLM、UMAP、更多模态补救定位。尚未获外部联系授权，不自动发邮件/issue 邀请；用户可在实际协作中收集反馈。

## 5. 实现后的科学测试策略

| 层面 | 必测案例 / 验收 |
|---|---|
| Metadata | 重复 ID 冲突、缺失 unit、category 拼写、numeric time 与 categorical time、悬空外键均有明确行为 |
| Rank/contrast | A/B/C、目标恒定、零列、完全线性组合、列置换、reference 改变；逐项有人工可算答案 |
| 重复单位 | 重复导出 cells/spots 不改变 design rank 与 unit counts；技术重复不能提高 biological n |
| 配对 | 完整配对、缺一侧、多次取样、batch 嵌套 unit；支持范围和 NOT_ASSESSED 正确 |
| 数值 | 数值协变量尺度、近共线、不同机器容差；不把浮点波动变成确定科学判断 |
| 不变量 | 样本行重排/ID 重命名不改变科学结果；target reference 改变不改变同一比较的可估计性 |
| 独立 oracle | 以 R model.matrix/独立 QR-SVD 计算的版本化小 fixtures 验证编码/可估计性；预期答案不能由被测函数生成 |
| CLI/E2E | clean venv 安装 wheel/sdist；所有 documented commands；退出码；不覆盖；HTML 与 JSON 一致 |
| 安全/报告 | YAML 不执行、HTML 字符转义、别名化、离线无外部请求、输出写入中断不伪造完整报告 |

科学 fixture 由人工解释与数值 oracle 双来源复核。只测路径覆盖率不算正确性。拒绝“mock 核心函数再测试调用次数”的自证测试。性能仅对已声明 metadata 规模测量。

## 6. 反馈记录模板（计划）

每例记录：使用者角色、日期、原始任务、实验单位定义、metadata 来源与可共享范围、目标 contrast、基线工具/版本、发现的问题、人工答案、耗时/手工步骤、误解、下一次是否会使用、结论。保留失败反馈。不得上传原始受限数据来完成产品验证。
